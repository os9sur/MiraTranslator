{
  "name": "mira_translator",
  "version": "3.3.6.1",
  "scripts": {
    "build:chrome": "node build.js --browser=chrome",
    "build:firefox": "node build.js --browser=firefox",
    "dev": "node build.js --browser=chrome --dev",
    "dev:firefox": "node build.js --browser=firefox --dev"
  },
  "dependencies": {
    "acorn": "^8.15.0",
    "fs-extra": "^11.3.3",
    "google-closure-compiler": "^20260218.0.0",
    "terser": "^5.46.0"
  }
}
